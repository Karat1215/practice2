# Часть 10 — Контрольные вопросы

Ответьте письменно. В вопросах с кодом — предскажите результат **до** запуска.

---

## Вопрос 1

Что выведет этот код? Объясните порядок выполнения блоков.

```java
class Demo {
    static { System.out.print("A "); }
    { System.out.print("B "); }
    Demo() { System.out.print("C "); }
    public static void main(String[] args) {
        System.out.print("D ");
        new Demo();
        System.out.print("E ");
        new Demo();
    }
}
```

**Предсказанный вывод:**

```
A D B C E B C
```

**Объяснение:**

1. static блок выполняется при загрузке класса → A

2. main() начинается → D

3. new Demo(): сначала блок инициализации экземпляра → B, затем конструктор → C

4. E

5. new Demo(): снова B C


---

## Вопрос 2

Класс `Child` в пакете `b`, класс `Parent` в пакете `a`. `Child extends Parent`. Какие члены `Parent` доступны в `Child`? А какие доступны в другом классе пакета `b`, который НЕ наследует `Parent`?

**Ваш ответ:**
В Child (наследник) доступны: public, protected, package-private (если Child в том же пакете, иначе нет)

В другом классе пакета b (не наследник) доступны только public


---

## Вопрос 3

Можно ли создать sealed-класс, у которого все наследники — record? Какое преимущество это даёт при использовании в switch?

**Ваш ответ:**

Да, можно. Преимущество: в switch с паттерн-матчингом компилятор проверяет полноту охвата всех наследников, обеспечивая безопасность.

---

## Вопрос 4

Что выведет этот код? Объясните, что такое String Pool и как работает `intern()`.

```java
String a = "Hello";
String b = new String("Hello");
String c = b.intern();
System.out.println(a == b);
System.out.println(a == c);
System.out.println(b == c);
```

**Предсказанный вывод:**

```
false
true
false
```

**Объяснение:**

a == b: a из пула строк, b создан через new — разные объекты → false

a == c: intern() возвращает ссылку из пула → true

b == c: b — объект в куче, c — из пула → разные → false


---

## Вопрос 5

Почему этот код не компилируется? Как исправить?

```java
interface A { default void hello() { System.out.println("A"); } }
interface B { default void hello() { System.out.println("B"); } }
class C implements A, B { }
```

**Ваш ответ:**
Конфликт default-методов — компилятор не знает, какой метод вызывать.
Исправление: переопределить метод в классе C:
```
class C implements A, B {

public void hello() {

A.super.hello();

    }

}
```

## Вопрос 6

В чём разница между `StringBuilder` и `StringBuffer`? Приведите конкретный сценарий, когда нужен именно `StringBuffer`.

**Ваш ответ:**

StringBuilder — несинхронизированный, быстрее

StringBuffer — синхронизированный, потокобезопасный, медленнее
Сценарий для StringBuffer: многопоточное приложение, где несколько потоков одновременно изменяют строку.
---

## Вопрос 7

Что выведет этот код? Объясните, что происходит с переменной `x` в лямбде.

```java
int x = 10;
Runnable r = () -> System.out.println(x);
// x = 20;  // что будет, если раскомментировать?
r.run();
```

**Предсказанный вывод:**

```
10
```

**Объяснение:**

Лямбда захватывает переменную x как effectively final. Если раскомментировать x = 20, будет ошибка компиляции, так как лямбда не может использовать изменяемую переменную.


---

## Вопрос 8

Напишите пример лямбда-выражения, которое нельзя заменить на ссылку на метод. Объясните почему.

**Ваш пример и объяснение:**

list.forEach(s -> System.out.println("Hello, " + s));

Нельзя заменить на ссылку на метод, так как лямбда выполняет операцию конкатенации с аргументом, а не просто вызывает метод с одним параметром.




---

## Вопрос 9

Чем `EnumMap` быстрее `HashMap` для ключей-enum? Опишите внутреннюю реализацию в одном предложении.

**Ваш ответ:**

EnumMap хранит данные в массиве, используя порядковый номер enum как индекс (O(1) без хеширования), что быстрее, чем вычисление хеша в HashMap.

---

## Вопрос 10

Дан `record Point(int x, int y) {}`. Что сгенерирует компилятор автоматически? Можно ли добавить в record изменяемое поле? Можно ли наследоваться от record?

**Ваш ответ:**

Компилятор генерирует: конструктор, equals(), hashCode(), toString(), геттеры x() и y()

Нельзя добавить изменяемое поле (record — неизменяемый)

Нельзя наследоваться от record (record — final)
